#!/bin/sh

set -eu

echo "Updating outdated Python packages, if any"
# shellcheck disable=SC2266
pip3 list --outdated --format=freeze | grep "==" || [ $? = 1 ] | cut -d = -f 1 |
    xargs -n1 pip3 install -U
