[mypy]
# Treat scripts as independent modules to avoid duplicate __main__ errors
scripts_are_modules = True
explicit_package_bases = True
# Ignore missing imports since these are standalone scripts
ignore_missing_imports = True
