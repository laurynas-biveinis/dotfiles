---
# Workflows deliberately use floating/unpinned action refs (e.g. @v6, @latest,
# @beta) per ai/.claude/rules/github-actions.md, so disable the unpinned-uses
# audit repo-wide instead of annotating each `uses:` line.
rules:
  unpinned-uses:
    disable: true
